import React from 'react';
import ReactDOM from 'react-dom/client';
import SetUp from './Project/Router/Routing';
import './index.css'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <>
    <SetUp />
  </>
);
